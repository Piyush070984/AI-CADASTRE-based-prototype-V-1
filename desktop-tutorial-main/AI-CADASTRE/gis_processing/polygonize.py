def polygonize_mask(mask_id: str) -> dict:
    return {
        "mode": "demo",
        "mask_id": mask_id,
        "polygons_created": 2,
        "note": "Deterministic demo polygonization output.",
    }
