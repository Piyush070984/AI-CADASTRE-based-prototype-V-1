# AI Methodology (Future)

- Prepare authorized imagery and label data.
- Train parcel-related segmentation model (e.g. U-Net).
- Evaluate with IoU, precision, recall, F1, and boundary quality.
- Integrate model inference into asynchronous worker pipeline.

Current implementation is deterministic demo mode.
