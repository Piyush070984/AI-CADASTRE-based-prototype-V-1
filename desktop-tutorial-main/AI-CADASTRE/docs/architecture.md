# Architecture

Frontend: React + TypeScript + Vite + Tailwind-like utility styling.
Backend: FastAPI + SQLAlchemy with PostgreSQL/PostGIS target and SQLite default fallback.
GIS/AI outputs are explicitly marked as demo until real authorized datasets and trained models are integrated.
